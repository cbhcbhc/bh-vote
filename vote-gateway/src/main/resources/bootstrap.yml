server:
  port: 8080
spring:
  application:
    name: vote-gateway
  profiles:
    active: dev

  cloud:
    nacos:
      discovery:
        server-addr: 139.159.162.93:8848
      #获取配置信息
      config:
        server-addr: 139.159.162.93:8848
        #从gateway命名空间下获取配置
        namespace: f638814c-34ea-44a9-adc8-8b57b281c21a
        # 配置文件后缀名
        file-extension: yml
        # 共享配置
        shared-configs:
          - application-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}