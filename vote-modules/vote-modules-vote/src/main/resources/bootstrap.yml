server:
  port: 9001
spring:
  application:
    name: vote-modules-vote
  profiles:
    active: dev

  cloud:
    nacos:
      discovery:
        server-addr: 139.159.162.93:8848
      #获取配置信息
      config:
        server-addr: 139.159.162.93:8848
        #从gateway命名空间下获取配置
        namespace: 856e9c8b-1464-475d-949e-145e9586dbb2
        # 配置文件后缀名
        file-extension: yml
        # 共享配置
        shared-configs:
          - application-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}